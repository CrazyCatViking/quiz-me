package component

templ Card() {
  <div class={ "card" }>
    { children... }
  </div>

  <style>
    .card {
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      padding: 1rem;

      border: 1px solid black;
      border-radius: 4px;

      background-color: var(--white);
    }
  </style>
}
